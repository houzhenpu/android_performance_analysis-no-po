if (rootProject.ext.config["applyPlugin"]) {
    // 引用插件
    //apply plugin: 'com.didi.dokit'
    // 这里引用正常库
    dependencies {
        //外部平台依赖
        api project(":doraemonkit")

    }
} else {
    // 引用no-op的库
    dependencies {
    }
}